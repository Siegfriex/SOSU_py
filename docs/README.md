# docs

- `../SSOT/SOSU_AI_SERVICE_SSOT_FINAL_20260913.md` — product/inference SSOT (owner-provided).
- Public API contract of record: `../contracts/openapi.json` + `../contracts/fixtures/*`.
  Where the SSOT and the session CONTRACT V1 disagree on field names / enum values, CONTRACT V1 wins.
