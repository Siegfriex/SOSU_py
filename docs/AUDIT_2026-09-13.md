# SOSU 전체 감사 보고 — 2026-09-13

대상: `sosu/` 모노레포 (상위 `web/` 프론트 · `SOSU_py/` Python AI 서비스 · `SSOT/` 문서)
작성: AI/Python 서비스 owner 세션 (write scope = `SOSU_py/**`, `web/`는 읽기만 함)
기준 커밋: 상위 `sosu@3915408` / `SOSU_py@5927c1b` + 미커밋 작업 트리

---

## 0. 한 줄 요약

| 영역 | 상태 |
|---|---|
| `SOSU_py` 백엔드 | **구현 완료, 전 게이트 통과.** ruff ✅ · mypy ✅ (40 files) · pytest **250 passed, 1 skipped** · `contracts/openapi.json` = 런타임 ✅ · 서버 실기동 ✅ (`/api/v1/health`, `/docs`, 에러 봉투) |
| `web/` 프론트 | **목데이터로 15개 상태 완주.** 백엔드 연동 미착수 — `web/src/api/mock.ts`가 인터페이스. `HANDOFF.md` §6에 "백엔드(`SOSU_py`) 실제 구조에 맞춰 조정"이라고 명시 |
| SSOT ↔ 백엔드 | **추론/파이프라인 원칙은 SSOT §23 acceptance gate 전부 충족.** wire 계약(필드명·코드·응답 구조·에러코드·경로)은 세션 프롬프트 CONTRACT V1을 따르며 SSOT §4~5/§13/§18~19와 **표기 차이** 있음 → 번역표를 `contracts/README.md` + `contracts/fixtures/ui_option_catalog.json`으로 제공 |
| SSOT ↔ 프론트 | **프론트는 SSOT wire 계약을 따르지 않음** (한글 라벨을 그대로 답변값으로 보관, 응답은 UI 문자열 모델). 프론트 에이전트는 SSOT를 "내용 확인 후 취급 결정"으로 남겨둠 |
| 커밋 | `SOSU_py` 작업 전부 **미커밋** (11개 최상위 경로, ~6,300 LoC). 상위 `web/`, `SSOT/`, `HANDOFF.md`도 untracked. push 없음 |
| `FRONTEND_CONTRACT_READY` | **YES** — `contracts/openapi.json` + `fixtures/*` 기준 (아래 §6 결정 사항 5건은 프론트 adapter 단에서 흡수 가능) |

---

## 1. 저장소 상태

```
sosu/                                 (Siegfriex/sosu, main, 3915408)
├─ .gitmodules                        SOSU_py → https://github.com/Siegfriex/SOSU_py.git, branch=main
├─ HANDOFF.md                         ?? (프론트 세션 산출물)
├─ SSOT/                              ?? (오너 추가; SOSU_py/SSOT 와 동일 파일 2개)
├─ web/                               ?? (Vite+React 19+TS, 56 files, 미커밋)
└─ SOSU_py/                           submodule (Siegfriex/SOSU_py, main, 5927c1b) — " m" (dirty)
   ├─ pyproject.toml, uv.lock, .python-version(3.12)
   ├─ .env.example / .env.local(gitignored, key=placeholder)
   ├─ app/ (41 py)  contracts/ (openapi.json, README.md, fixtures×17)  scripts/ (3)  tests/ (13 + helpers)
   ├─ SSOT/ (오너 추가, untracked)  docs/ (README.md, 본 문서)
```

- 중첩 git 저장소 생성 없음 (SOSU_py는 submodule 하나뿐). 상위 repo의 무관한 변경(`web/`, `SSOT/`, `HANDOFF.md`)은 건드리지 않음.
- 백엔드 커밋/푸시는 오너 지시가 없어 보류. 커밋 시 `SOSU_py`에서 커밋 → 상위에서 `git add SOSU_py` 포인터 갱신 순.

## 2. 환경

| 항목 | 값 |
|---|---|
| Python | 3.12.3 (system) — uv 0.10.8 로 `.venv` 관리 |
| 주요 의존성 | fastapi 0.141.1 · pydantic 2.13.5 · pydantic-settings 2.15 · google-genai **2.23.0** · pillow 12.3 · uvicorn 0.52.4 · httpx 0.28 · tenacity 9.1 |
| dev | pytest 9.1 · pytest-asyncio 1.4 · ruff 0.16.7 · mypy 2.3.1 |
| 금지 의존성 | LangChain/LlamaIndex/agent framework 없음 |
| 포트 | 8000(API), 5173(Vite) 비어 있음 |
| 환경변수 | `.env.local` = 프롬프트 §5 그대로 (`gemini-3.8-flash`, thinking low/medium/low, max_output 2048/8192/4096/4096, CORS 5173, `RUN_LIVE_GEMINI_TESTS=0`) |
| Gemini | `gemini_configured=false` (placeholder). **live 호출 0회** — 테스트/스모크 모두 mock |

## 3. 백엔드 구현 요약 (SOSU_py)

### 3.1 엔드포인트
| 메서드/경로 | 입력 | 출력 |
|---|---|---|
| `GET /api/v1/health` | — | `{status, service:"sosu-ai", contract_version:"1.0", gemini_configured}` (키 미노출) |
| `POST /api/v1/diagnosis` | multipart: `payload`(JSON `SurveyDiagnosisRequestV1`) + `product_image` 0..1 | `SuccessEnvelope<DiagnosisReportV1>` |
| `POST /api/v1/prescription` | multipart: `payload`(JSON `PrescriptionRequestV1`) + `insight_images` ×3 | `SuccessEnvelope<PrescriptionReportV1>` |

봉투: `{ok, request_id, contract_version, data | error{code,message,retryable,details}}`. `X-Request-ID` 수용/에코. 에러 12종 → HTTP 413/415/422/500/502/504 매핑 (`contracts/README.md` 표).

### 3.2 계약 모델 (`app/contracts/`)
- `enums.py` 7종 (ProductAppeal, PurchaseMotive, DesiredEmotion, **BrandAttribute(별도 타입)**, InstagramPainPoint, CurrentReelFormat, CreatorDisclosure)
- `SurveyAnswersV1` — `extra="forbid"` (q19 거부), 리스트 max 3, 텍스트 max 4000, 전 필드 optional
- `DiagnosisReportV1` — priorities 정확히 3(rank 1|2|3), reel_types 정확히 3, structure 5슬롯, `schema_version:"diagnosis.v1"`
- `PrescriptionRequestV1` — `instagram_url` host ∈ instagram.com, path `/reel|reels|p/<id>` 검증
- `PrescriptionReportV1` — next_actions 정확히 4(order 1..4), `schema_version:"prescription.v1"`
- `catalog.py` — 한글 UI 라벨 → 코드 (프론트 `questions.ts`의 options와 문자열 일치 확인, 단 Q17 "사진/영상 혼합"은 카탈로그에서 "사진·영상 혼합" → §6-③ 참조), `SSOT_CODE_ALIASES`

### 3.3 파이프라인 (`app/domain`, `app/gemini`, `app/services`)
```
SurveyAnswersV1 → normalize_survey (NFC·CRLF→LF·trim·빈값→null·2000자 안전길이, 의미 재작성 없음)
  → DeclaredEvidenceV1(+CreatorConstraint from Q18)
  → [이미지 있으면] Gemini visual-observation-v1 (문진 미주입, 이미지만) → VisualObservationV1
  → build_evidence_pack(+INFERENCE_POLICY_V1 §12 라우팅) → Gemini diagnosis-v1 → DiagnosisDraftV1
  → validate_diagnosis (PASS/REPAIRABLE/FATAL) → REPAIRABLE이면 repair-v1 **정확히 1회** → 재검증
  → to_public_diagnosis → DiagnosisReportV1
```
처방전: 스크린샷 3장 → insight-observation-v1 → InsightObservationV1 → prescription-v1 → 검증/1회 수리 → PrescriptionReportV1. URL은 프롬프트에 "미관측 식별자"로만 명시.

- `GoogleGenAIGateway` (SDK 격리): `client.aio.models.generate_content(..., response_mime_type="application/json", response_json_schema=<sanitized pydantic schema>, thinking_config=ThinkingConfig(thinking_level=LOW|MEDIUM|HIGH), max_output_tokens, http_options.timeout)`. `asyncio.wait_for` + tenacity(429/500/503만 2회). 오류 매핑: timeout→MODEL_TIMEOUT, APIError→MODEL_PROVIDER_ERROR, 빈 후보/SAFETY/MAX_TOKENS→MODEL_CONTENT_ERROR, JSON/스키마 불일치→MODEL_SCHEMA_ERROR. usage 토큰·latency INFO 로그, 키/사용자 텍스트 미로깅, `include_thoughts` 미설정.
- `validator.py`: 빈 텍스트·placeholder(`...`, N/A, TBD, 미정)·rank/order 중복·키워드 빈 리스트 → REPAIRABLE; **Q18 위반**(face 금지 시 얼굴/표정/셀피/on-camera…, voice 금지 시 내레이션/보이스오버/목소리/narration…) 14자 부정문 창("없이/대신/하지 않/without") 처리.
- 이미지 (`app/images/normalize.py`): MIME(jpeg/png/webp) + Pillow 실제 포맷 검사, EXIF transpose, RGB(알파→흰 배경), LANCZOS 축소(제품 1600 / 인사이트 2000 long edge, 업스케일 없음), JPEG q85/q90 → 목표 700KB/900KB까지 5씩 감소(하한 60/75), 메타데이터 제거, 메모리 내 처리만.

### 3.4 테스트 (250 passed, 1 skipped — 2026-09-13 15:xx 실행)
| 파일 | 수 | 프롬프트 §22 항목 |
|---|---:|---|
| test_normalizer.py | 41 | D, E, F, G |
| test_reports.py | 40 | J, K, URL 검증 |
| test_images.py | 36 | H, I |
| test_enums.py | 35 | A, B, C |
| test_api.py | 23 | O |
| test_openapi.py | 20 | P (+fixture 검증, face_only 일관성) |
| test_gemini_client.py | 19 | 스키마 sanitizer, 오류 매핑 (네트워크 없음) |
| test_validator.py | 14 | L |
| test_evidence_pack.py | 9 | 라우팅 9섹션 |
| test_pipelines.py | 5 | N (visual call에 문진 미포함 증명 포함) |
| test_ssot_aliases.py | 4 | SSOT §5 코드 ↔ V1 코드 1:1 |
| test_repair.py | 4 | M (2회째 호출 없음) |
| test_live_gemini.py | 1 skipped | `RUN_LIVE_GEMINI_TESTS=1` + 키 필요 |

## 4. 프론트(`web/`) 현상태 — 읽기 전용 관찰

- 스택: Vite 8 · React 19.2 · TS 6 · react-router 7. `npx tsc -b` 0 error (HANDOFF §7). 배포 없음.
- 상태머신: SURVEY `STEP_1→2→3→UPLOAD→GENERATING→RESULT→DELIVERY→DELIVERED(+ERROR)`, PRESCRIPTION `INPUT→GENERATING→…`. sessionStorage 영속, 파일은 메모리 레지스트리.
- **API 계층 = `src/api/mock.ts`** (`generateDiagnosis(answers, {onStage, signal})`, `generatePrescription(url, …)`, `sendReportEmail(email)`). 실제 HTTP 호출 코드 없음.
- **답변 모델 = `Answers: Record<'q01'…'q20', string | string[]>`** — 선택형 값은 **한글 라벨 문자열 그대로** (`'디자인'`, `'얼굴만'`). stable code 변환 없음.
- **리포트 모델 = UI ViewModel** (`SurveyReport`: strengths/target/tone/finalGuidance가 단일 문자열, structure `{range, guide}[]`, `guidebookNote` 상수, `fonts{title, subtitle}`; `PrescriptionReport`: prescription 문자열, nextActions string[], tone 문자열). HANDOFF §6은 "Gemini 출력과 다를 수 있으므로 **adapter 계층에서 변환**"으로 설계.
- 파일 정책: `fileStore.put` 유효 MIME = `image/jpeg|image/png|application/pdf`(!) — 처방전 FileUploader accept `image/jpeg,image/png`, max 3. 문진 업로드 개수/용량 상한 미정(OPEN L8).
- URL 검증: `instagram.com` 호스트만 (path 미검사).
- 로딩: stage 1~4 이벤트(입력 확인/이미지 분석/진단 생성/결과 정리) 또는 indeterminate. 숫자 % 금지.
- HANDOFF §6이 제안한 HTTP 형태(`POST /api/diagnosis → {jobId}` + 폴링)는 **"제안"**이며 "`SOSU_py` 실제 구조에 맞춰 조정"이라 명시.

## 5. SSOT 정합성 3자 비교

### 5.1 추론/파이프라인 원칙 (SSOT가 지배) — 백엔드 충족 여부
| SSOT §23 acceptance gate | 백엔드 | 근거 |
|---|:-:|---|
| categorical → stable enum 검증 | ✅ | enums + `extra="forbid"` + test_enums |
| 자연어 원문 보존, semantic slot 배치 | ✅ | normalizer (NFC/trim/CRLF/빈값/길이만), test_normalizer E |
| Q10/Q11 별도 namespace | ✅ | `DesiredEmotion` ≠ `BrandAttribute`, 두 $defs |
| Q18 hard constraint 파생 | ✅ | `CreatorConstraint.from_disclosure` 4케이스 |
| Q19 부재 | ✅ | 필드 없음 + 거부 테스트 |
| 이미지 있으면 image-only observation 선행 | ✅ | test_pipelines: 1번째 call에 문진 문자열 부재 |
| 이미지 없으면 visual 생략, prior-only | ✅ | 1 call 경로 테스트 |
| section별 evidence routing | ✅ | `INFERENCE_POLICY_V1` 9섹션 (프롬프트 §12 표 기준) |
| JSON structured output / regex·XML 파싱 없음 | ✅ | `response_json_schema`, `model_validate_json`만 |
| schema 후 semantic validator | ✅ | validator.py |
| repair 최대 1회 | ✅ | test_repair M |
| malformed partial을 success로 반환 안 함 | ✅ | REPAIR_FAILED/MODEL_* 에러 봉투 |
| 이메일/버튼/navigation 미주입 | ✅ | payload 스키마에 해당 필드 없음 (`extra="forbid"`) |
| 처방 URL을 fetched evidence로 오인 금지 | ✅ | prescription 프롬프트에 "미관측" 명시, 크롤링 코드 없음 |
| 읽히지 않는 metric 추정 금지 | ✅ | `unreadable_regions`, 프롬프트 지시 |
| 외부 저장소 없이 동작 | ✅ | BytesIO만, GCS/DB 없음 |
| SSOT §11 guard(인구통계 과잉추정 금지·폰트 라이선스 창작 금지·실행 불가 제안 금지), §15 분량 가이드, §11.1 gap 예시 | ✅ | 이번 감사에서 `diagnosis.py` SYSTEM 블록에 추가 (규칙 12~14 + 분량 가이드) |

### 5.2 wire 계약 — 표기 차이 (프롬프트 CONTRACT V1이 지배, SSOT는 "product/inference SSOT")
| 항목 | SSOT | 백엔드(CONTRACT V1, 구현) | 프론트(현재) |
|---|---|---|---|
| 경로 | `/v1/diagnosis` | `/api/v1/diagnosis` | 없음(mock) / 제안 `/api/diagnosis` |
| 요청 필드 | `brand_name`, `appeal_codes`, `exposure_mode` … + `*_other_text` | `q1_brand_name`, `q6_product_appeals`, `q18_disclosure` … (other_text 없음) | `q01`…`q20` 키, 값=한글 라벨 |
| 코드 | `making_process`, `special`, `unique_ownership`, `being_moved`, `low_views`, `face_and_voice`… | `process`, `specialness`, `self_expression`, `moved`, `low_reach`, `yes`… | 코드 없음(라벨) |
| 제품 이미지 | `product_images[]` 0..N | `product_image` 0..1 | 0..N(상한 미정 L8) |
| 응답 | `{schema_version:"diagnosis-report.v1", status, data:{brand_summary, first_show_priorities, reel_type_mix, reel_structure[{slot,…}], final_guidance{summary,checklist}}}` | `{ok, request_id, contract_version, data:{brand, priorities, reel_types, structure{hook_0_3…}, final_guidance[]}}` | UI 문자열 ViewModel |
| 처방 응답 | `next_actions: string[4]` | `next_actions: [{order,title,description}]×4` | `nextActions: string[]` |
| 에러코드 | `INPUT_INVALID, INPUT_INSUFFICIENT, UNSUPPORTED_MEDIA, PAYLOAD_TOO_LARGE, IMAGE_UNREADABLE, MODEL_RATE_LIMIT, MODEL_UPSTREAM_ERROR, MODEL_OUTPUT_INVALID` | `INVALID_REQUEST, INSUFFICIENT_INPUT, INVALID_IMAGE_TYPE, IMAGE_TOO_LARGE, INVALID_IMAGE_COUNT, INVALID_INSTAGRAM_URL, MODEL_TIMEOUT, MODEL_PROVIDER_ERROR, MODEL_SCHEMA_ERROR, MODEL_CONTENT_ERROR, REPAIR_FAILED, INTERNAL_ERROR` | `errorKind: 'timeout' \| 'error'` |
| 디렉토리 | `services/ai`, `packages/contracts` | `SOSU_py/`, `SOSU_py/contracts/` | `web/` (SSOT `apps/web` 아님) |

판단: **세 문서 중 어느 둘도 wire에서 일치하지 않으며, 프론트는 아직 wire를 갖고 있지 않다.** 따라서 실질 wire SSOT는 백엔드가 내보낸 `contracts/openapi.json` + `fixtures/*`이고, 프론트 adapter(HANDOFF §6이 예정한 계층)가 ① 라벨→코드(`ui_option_catalog.json`), ② 구조화 리포트→ViewModel 변환을 담당하는 것이 최소 변경 경로다. SSOT 코드명으로 wire를 재작성하는 것은 양쪽 모두에 추가 작업이며 이득이 없다(프론트가 SSOT 코드를 쓰지 않으므로).

## 6. 결정 필요 사항 (오너)

| # | 항목 | 현재 | 제안 |
|---|---|---|---|
| ① | **wire 계약 확정** | 백엔드 CONTRACT V1 구현 완료, SSOT와 표기 차이 | `contracts/openapi.json`을 wire SSOT로 선언. SSOT 문서 §4~5/§13/§18~19를 V1 표기로 갱신하거나 "번역표 참조" 각주 추가 |
| ② | 문진 제품 이미지 개수 | SSOT 0..N / 백엔드 0..1 / 프론트 0..N(L8 미정) | v1은 **1장**(비용·관측 단순화). N장이면 백엔드 `product_images` 리스트 + 합산 예산 + visual call 1회에 N장 — 30분 작업 |
| ③ | Q17 라벨 | 프론트 `"사진/영상 혼합"` vs 카탈로그 `"사진·영상 혼합"` | 프론트 문자열 기준으로 카탈로그 수정(1줄) — 오너 확인만 필요 |
| ④ | Q9 최대 선택 | 프론트 max 없음(L4) / 백엔드 max 3 | 프론트 `maxSelection: 3` 권장(프롬프트 §7 규정) |
| ⑤ | 업로드 MIME | 프론트 `application/pdf` 허용(문진) / 백엔드 이미지만 | 프론트에서 AI 업로드는 jpeg/png(/webp)만. PDF는 415 `INVALID_IMAGE_TYPE` |
| ⑥ | Instagram URL 규칙 | 프론트 host만 / 백엔드 `/reel|reels|p/` path 필수 | 백엔드 규칙 채택(프론트 `isValidInstagramUrl` 동기화) 또는 백엔드 완화 — 한쪽 1줄 |
| ⑦ | `*_other_text` (SSOT §4.2) | 백엔드 없음(`extra="forbid"`로 거부) | 프론트에 "기타" 자유입력 UI가 없으면 불필요. 필요 시 4필드 추가(q6/q10/q11/q16) — 15분 |
| ⑧ | stage 이벤트 | 백엔드 동기 단일 응답 | v1은 프론트 indeterminate(`stage=null`, HANDOFF가 허용). SSE는 다음 단계 |
| ⑨ | 클라이언트 타임아웃 | 백엔드 Gemini 호출당 90s, 최대 3회(관측+합성+수리) | 프론트 AbortSignal ≥ 240s 권장 |

## 7. 프롬프트 §26 최종 보고 항목

1. **repository state** — §1. 백엔드 미커밋, 중첩 repo 없음, 상위 무관 변경 미접촉.
2. **files created** — `app/` 41 · `tests/` 16 · `scripts/` 4 · `contracts/` openapi.json + README.md + fixtures 17 · `.env.example` · `.env.local`(gitignored) · `.gitignore` · `pyproject.toml` · `uv.lock` · `README.md` · `docs/README.md` · 본 문서.
3. **dependency/version state** — §2.
4. **environment status** — `.env.local` placeholder, `gemini_configured=false`, live 호출 0.
5. **endpoint list** — §3.1.
6. **Pydantic contracts** — §3.2.
7. **Gemini pipeline** — §3.3 (prompt versions: visual-observation-v1 · diagnosis-v1 · insight-observation-v1 · prescription-v1 · repair-v1).
8. **image normalization** — §3.3 마지막 항.
9. **validator/repair** — PASS/REPAIRABLE/FATAL, 수리 1회, 실패 시 `REPAIR_FAILED`(502, retryable).
10. **tests** — `uv run pytest` → **250 passed, 1 skipped** (live), `ruff check .` clean, `mypy` clean(40 files).
11. **OpenAPI/fixture paths** — `SOSU_py/contracts/openapi.json`, `SOSU_py/contracts/fixtures/{diagnosis.request, diagnosis.success, prescription.request, prescription.success, ui_option_catalog, error.×12}.json`, `SOSU_py/contracts/README.md`. `scripts/check_openapi.py` → "matches runtime".
12. **live Gemini smoke** — **SKIPPED** (키 placeholder). 키 삽입 후 `RUN_LIVE_GEMINI_TESTS=1 uv run pytest tests/test_live_gemini.py`.
13. **blockers** — 없음(코드). 결정 대기 = §6 ①~⑨. 특히 ① wire 확정과 ② 이미지 개수는 프론트 adapter 착수 전에 필요.
14. **FRONTEND_CONTRACT_READY = YES** — `contracts/openapi.json`이 런타임과 일치하고 fixture가 모델 검증을 통과함. 단 §6 ①의 "SSOT를 wire로 재작성" 결정이 내려지면 NO로 되돌아가며 재작성 소요 약 30~40분.

## 8. 재현 커맨드

```bash
cd /home/sieg/projects-wsl/sosu/SOSU_py
uv sync
uv run ruff check . && uv run mypy
uv run python scripts/export_openapi.py && uv run python scripts/check_openapi.py
uv run python scripts/make_fixtures.py
uv run pytest                                   # 250 passed, 1 skipped
uv run uvicorn app.main:app --reload --port 8000
curl http://127.0.0.1:8000/api/v1/health        # {"status":"ok",...,"gemini_configured":false}
```
